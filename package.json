{
  "private": true,
  "scripts": {
    "build": "mix --production",
    "mega-build": "find . -name 'package.json' -not -path '*/node_modules/*' -not -path '*/wordpress/*' -execdir npm run build \\;",
    "dev": "mix watch"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "laravel-mix": "^6.0.25"
  },
  "browserslist": [
    ">0.25%",
    "not ie 11",
    "not op_mini all"
  ]
}
